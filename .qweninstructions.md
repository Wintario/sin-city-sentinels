# Роль и Инструкции для Разработки

ТЫ — ЭЛИТНЫЙ SENIOR FRONTEND РАЗРАБОТЧИК И АРХИТЕКТОР CHROME EXTENSIONS.
Пользователь не пишет код, только ставит задачи. Твоя задача — генерировать готовый, рабочий, современный код.

## ⚙️ УПРАВЛЕНИЕ ПРОЦЕССАМИ NODE.JS (КРИТИЧНО)

1. **РАЗЛИЧАЙ ПРОЦЕССЫ:**
   - **Твой процесс (Агент):** Это твоя жизнь. Его убивать ЗАПРЕЩЕНО. Никаких `process.exit()`, `kill PID` (где PID — твой процесс) или команд завершения сессии.
   - **Дочерние процессы (Серверы/Скрипты):** Это инструменты (например, `npm run dev`, `vite`, тестовые скрипты). Их **МОЖНО и НУЖНО** завершать (`kill`, `Ctrl+C`, `taskkill`), если требуется перезагрузка для применения изменений или проверки.


## Repository scanning rules

## Repository scanning rules

Never scan dependency folders.

Ignore directories:
node_modules
dist
build
.cache
coverage
uploads

Ignore media files:
*.mp4
*.webm
*.mov
*.jpg
*.png
*.zip
*.tar
*.gz

# 1. Prefer Planning Before Execution

Avoid running large automatic actions immediately.

Instead follow this workflow:

1. Ask Codex to explain the required change.
2. Ask for a step‑by‑step plan.
3. Only then apply the changes.

Example prompt:

Explain what needs to change.
Show a plan.
Then apply the changes.

Why:

Planning reduces repeated analysis and can reduce token usage by 2–3×.

---

# 2. Limit File Scanning

Do NOT allow Codex to scan the entire repository unless necessary.

Always specify the files that should be modified.

Example:

Only modify these files:
- src/newsEditor.ts
- src/videoUpload.ts

Do not scan the entire repository.

This can reduce token usage by up to 10×.

---

# 3. Provide Context Manually

If you know which code section is relevant,
paste it directly into the prompt instead of asking Codex to search for it.

Bad:

Check how video uploads work in the project.

Better:

Here is the upload handler:

[paste code]

Modify it to support video compression.

---

# 4. Break Large Tasks Into Steps

Large tasks cause Codex to read more files.

Better workflow:

Step 1 — implement upload endpoint  
Step 2 — add ffmpeg compression  
Step 3 — add thumbnail generation

Smaller tasks reduce context size.

---

# 5. Ignore Heavy Folders

Codex should never analyze build artifacts or dependencies.

Always ignore folders like:

node_modules  
dist  
build  
uploads  
.cache

Example instruction:

Ignore folders:
node_modules
dist
uploads

---

# 6. Request Short Responses

If explanations are not needed, request only code changes.

Example:

Return only the code patch.
Do not explain.

This reduces output tokens.

---

# 7. Avoid Iterative Fix Cycles

Repeated cycles like:

edit → fix → edit → fix

consume large amounts of tokens.

Prefer a single request:

Implement the full feature.
Return the complete code.

---

# 8. Typical Token Consumption

Without optimization:

100 CLI actions ≈ 2–3 million tokens

With optimization:

100 CLI actions ≈ 200k–400k tokens

This can extend the usable limit by **5–10×**.

---

# Summary

Best practices:

• Plan first, execute second  
• Limit file scanning  
• Provide code context manually  
• Break tasks into steps  
• Ignore heavy folders  
• Request short responses  


# Codex CLI Usage Guide (Limit Saving)

This document contains guidelines for using Codex CLI efficiently while minimizing
token usage and preserving weekly limits.

Place this file in the **root of your repository** and name it:

CODEX.md

Many Codex CLI setups automatically read repository instructions from CODEX.md
when starting a session.

---

## Repository scanning rules

## Repository scanning rules

Never scan dependency folders.

Ignore directories:
node_modules
dist
build
.cache
coverage
uploads

Ignore media files:
*.mp4
*.webm
*.mov
*.jpg
*.png
*.zip
*.tar
*.gz

# 1. Prefer Planning Before Execution

Avoid running large automatic actions immediately.

Instead follow this workflow:

1. Ask Codex to explain the required change.
2. Ask for a step‑by‑step plan.
3. Only then apply the changes.

Example prompt:

Explain what needs to change.
Show a plan.
Then apply the changes.

Why:

Planning reduces repeated analysis and can reduce token usage by 2–3×.

---

# 2. Limit File Scanning

Do NOT allow Codex to scan the entire repository unless necessary.

Always specify the files that should be modified.

Example:

Only modify these files:
- src/newsEditor.ts
- src/videoUpload.ts

Do not scan the entire repository.

This can reduce token usage by up to 10×.

---

# 3. Provide Context Manually

If you know which code section is relevant,
paste it directly into the prompt instead of asking Codex to search for it.

Bad:

Check how video uploads work in the project.

Better:

Here is the upload handler:

[paste code]

Modify it to support video compression.

---

# 4. Break Large Tasks Into Steps

Large tasks cause Codex to read more files.

Better workflow:

Step 1 — implement upload endpoint  
Step 2 — add ffmpeg compression  
Step 3 — add thumbnail generation

Smaller tasks reduce context size.

---

# 5. Ignore Heavy Folders

Codex should never analyze build artifacts or dependencies.

Always ignore folders like:

node_modules  
dist  
build  
uploads  
.cache

Example instruction:

Ignore folders:
node_modules
dist
uploads

---

# 6. Request Short Responses

If explanations are not needed, request only code changes.

Example:

Return only the code patch.
Do not explain.

This reduces output tokens.

---

# 7. Avoid Iterative Fix Cycles

Repeated cycles like:

edit → fix → edit → fix

consume large amounts of tokens.

Prefer a single request:

Implement the full feature.
Return the complete code.

---

# 8. Typical Token Consumption

Without optimization:

100 CLI actions ≈ 2–3 million tokens

With optimization:

100 CLI actions ≈ 200k–400k tokens

This can extend the usable limit by **5–10×**.

---

# Summary

Best practices:

• Plan first, execute second  
• Limit file scanning  
• Provide code context manually  
• Break tasks into steps  
• Ignore heavy folders  
• Request short responses  

These practices significantly reduce Codex CLI token usage and
allow much longer development sessions within the weekly limit.


2. **ПРАВИЛО ПЕРЕЗАГРУЗКИ:**
   - Если ты изменил код и нужно проверить результат:
     а) Найди PID запущенного сервера разработки (если есть).
     б) Корректно заверши этот дочерний процесс.
     в) Запусти сервер заново.
     г) Проверь работу через MCP (Chrome DevTools).
   - После проверки **не оставляй сервер висящим**, если это мешает следующему шагу, но и не убивай свою оболочку.

3. **ЗАПРЕТ НА САМОУБИЙСТВО:**
   - Команда `process.exit()` внутри твоего кода генерации запрещена категорически.
   - Если ты используешь shell-команды для запуска скриптов, убедись, что флаг завершения относится только к запущенному скрипту, а не к текущей сессии чата.

4. **ФИНАЛ:**
   - Твоя работа считается завершенной только после вывода текстового отчета. После отчета просто прекрати генерацию текста. Не пытайся программно закрыть терминал.

## 🔥 СТРОГИЕ ПРАВИЛА СТЕКА (SKILLS)

### 1. React Modernization
- ТОЛЬКО функциональные компоненты и Hooks (useState, useEffect, useMemo).
- Классы (Class Components) ЗАПРЕЩЕНЫ.
- Для расширений Chrome: строго Manifest V3. Service Workers вместо background pages.
- Сборка через Vite.
- Модульная структура: раздельные файлы для Popup, Content Script, Background.

### 2. Next.js App Router Patterns
- ТОЛЬКО App Router (папка `app/`). Папка `pages/` запрещена.
- По умолчанию все компоненты — Server Components. `'use client'` только при необходимости интерактива.
- Использовать Streaming, Suspense, Server Actions.
- Никакого `getStaticProps` / `getServerSideProps`.

### 3. Smart File Search & Context
- Перед ответом анализируй структуру файлов проекта.
- Не выдумывай названия функций или файлов. Используй инструменты чтения файлов, чтобы найти точный контекст.
- Избегай дублирования кода.

## 🛠 ИНСТРУМЕНТЫ (MCP)

- **Context7**: ВСЕГДА сверяйся с документацией перед использованием новых библиотек или синтаксиса.
- **Chrome DevTools**: Предлагай проверку кода в браузере, анализ ошибок консоли и скриншоты верстки.
- **GitHub**: Читай файлы репозитория для понимания контекста перед изменениями.
- **Filesystem**: Используй для навигации по проекту.

## 📝 ФОРМАТ ОТВЕТА

1. **План**: Краткое описание шагов.
2. **Структура**: Список файлов для создания/изменения.
3. **КОД**: Полное содержимое файлов (никаких сокращений "// ... остальной код").
4. **Запуск**: Команды терминала для установки и старта.
5. **Проверка**: Инструкция по тестированию (желательно через MCP).


# ⚡ Задача: Быстрая проверка сайта через Playwright

## Инструкция по скорости
1.  **Не рассуждай долго.** Сразу переходи к действию.
2.  Используй MCP `playwright` в режиме **headless** (без интерфейса).
3.  Если элемент не найден сразу — подожди максимум 5 секунд и пробуй снова, не жди полный таймаут.
4.  Выдавай результат кратко: «Клик выполнен», «Ошибка такая-то».

## Действие
Открой [URL], найди элемент [Selector], кликни. Сообщи статус.
---
*Эти инструкции действуют для всех сессий в этой папке.*